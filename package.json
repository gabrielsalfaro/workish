{
  "name": "workish",
  "version": "1.0.0",
  "description": "This app is a job board service to allow job seekers to view job listings from potential employers. Employers are able to create job postings and connect with potential candidates.",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "install": "npm --prefix backend install backend",
    "dev:backend": "npm install --prefix backend start",
    "sequelize": "npm run --prefix backend sequelize",
    "sequelize-cli": "npm run --prefix backend sequelize-cli",
    "start": "npm start --prefix backend",
    "build": "npm run --prefix backend build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {}
}
